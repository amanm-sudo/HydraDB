{
  "name": "blast-radius-server",
  "version": "1.0.0",
  "description": "blast-radius API server — supply chain compromise graph queries over HydraDB",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "fastest-levenshtein": "^1.0.16",
    "neo4j-driver": "^5.28.1"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
